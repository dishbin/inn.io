import DirectMessagesContainer from "../container/DirectMessagesContainer";

export default function DirectMessages({}) {
    return(
        <div className='DirectMessages'>
            <DirectMessagesContainer />
        </div>
    );
}